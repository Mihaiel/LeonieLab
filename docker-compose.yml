services:
  leonielab:
    image: nginx:alpine
    container_name: leonielab
    volumes:
      - ./public:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "8082:80"
    restart: unless-stopped
